# the code defines lambda function named upper that takes 
# a string as its argument and cinverts it to uppercase using upper( method)
# it then applies this lambda function to the string


str1 = 'GeeksforGeeks'

upper = lambda string: string.upper()
print(upper(str1))
